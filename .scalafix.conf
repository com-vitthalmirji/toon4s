rules = [DisableSyntax]

DisableSyntax {
  regex = [
    {
      id = "spark-driver-collect"
      pattern = "\\.collect\\s*\\("
      message = "Do not use collect(). Use distributed operations or bounded take(...)."
    }
  ]
}
